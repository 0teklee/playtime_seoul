import Layout from "components/common/Layout";
import AdmissionContent from "components/Pages/Admission/AdmissionContent";

const admission = () => {
  return (
    <Layout>
      <AdmissionContent />
    </Layout>
  );
};

export default admission;
